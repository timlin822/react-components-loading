import './Loading.css';

const LoadingProgressBar=()=>{
    return (
        <div className="loading-progress-bar">
            <h1 className="loading-progress-bar-text">Loading...</h1>
        </div>
    );
}

export default LoadingProgressBar;